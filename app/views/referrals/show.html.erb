Student: <%= @referral.student.first_name %>
<br>
Teacher: <%= @referral.user.name %>

<p>
  <strong>Note:</strong>
  
  <%= @referral.note %>
</p>

<%= link_to 'Edit', edit_student_referral_path(@referral) %> |
<%= link_to 'Back', student_path(@student) %>
