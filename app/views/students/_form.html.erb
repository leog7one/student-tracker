<%= form_with(model: student, local: true) do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :student_first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :student_last_name %>
  </div>

  <div class="field">
    <%= form.label :student_id_number %>
    <%= form.number_field :student_id_number, id: :student_student_id_number %>
  </div>

  <div class="field">
    <%= form.label :grade_level %>
    <%= form.number_field :grade_level, id: :student_grade_level %>
  </div>

  <div class="field">
    <%= form.fields_for :homerooms do |hr| %>
      <%= hr.label :room_number %>
      <%= hr.text_field :room_number %>
    <% end %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
